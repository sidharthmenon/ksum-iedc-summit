image: node:latest

cache:
  paths:
    - node_modules/

install_dependencies:
  stage: build
  script:
    - npm install
  artifacts:
    paths:
      - node_modules/

pages:
  script:
    - npm run build
  artifacts:
    paths:
      - dist/